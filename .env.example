# Copy to .env and fill in. The app loads .env on startup; real environment
# variables override it. .env is gitignored — never commit real secrets.
ANTHROPIC_API_KEY=sk-ant-...
SPOTIFY_ID=your-spotify-client-id
SPOTIFY_SECRET=your-spotify-client-secret
